# Run any command in this project's bin/ without the bin/ prefix
PATH_add exe
PATH_add bin

# This project uses mise.toml for environment management.
# Install/activate mise, or run commands with:
#   mise exec -C /path/to/project -- <command>
true
